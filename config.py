"""
Централізована конфігурація Job Search Automation Pipeline.

Усі "бізнес-правила", які раніше жили тільки в тексті промпту scheduled-задачі,
тепер живуть тут як звичайні Python-константи — редагувати критерії means
редагувати цей файл, а не переписувати промпт вручну.
"""
from __future__ import annotations

import os
from dataclasses import dataclass, field

from dotenv import load_dotenv

load_dotenv()  # читає .env у робочій директорії, якщо він є

# --------------------------------------------------------------------------
# Секрети / оточення
# --------------------------------------------------------------------------
# Anthropic API ключ читається зі змінної середовища ANTHROPIC_API_KEY
# (див. .env.example). Google credentials — з credentials/credentials.json
# (OAuth client secret, завантажений з Google Cloud Console).
ANTHROPIC_API_KEY = os.environ.get("ANTHROPIC_API_KEY", "")
CLAUDE_MODEL = os.environ.get("CLAUDE_MODEL", "claude-sonnet-4-5-20250929")

GOOGLE_CREDENTIALS_PATH = os.environ.get(
    "GOOGLE_CREDENTIALS_PATH", "credentials/credentials.json"
)
GOOGLE_TOKEN_PATH = os.environ.get("GOOGLE_TOKEN_PATH", "credentials/token.json")

# Scopes, потрібні пайплайну. drive.file обмежує доступ лише файлами,
# створеними/відкритими цим додатком — але оскільки нам треба читати
# наявну папку з резюме, потрібен повний drive.readonly + drive для
# читання/запису службових файлів.
GOOGLE_SCOPES = [
    "https://www.googleapis.com/auth/drive",
    "https://www.googleapis.com/auth/documents",
    "https://www.googleapis.com/auth/spreadsheets",
    "https://www.googleapis.com/auth/gmail.modify",
]

# --------------------------------------------------------------------------
# Google Drive / Docs / Sheets ідентифікатори
# --------------------------------------------------------------------------
RESUME_FOLDER_ID = "1R_IeQaYOxf8EX6J1CUZpUjcvZaFMJ-7n"  # "Резюме для працевлаштування..."

# --------------------------------------------------------------------------
# Паралельне звірення з чат-версією (тимчасовий режим)
# --------------------------------------------------------------------------
# Поки Python-пайплайн не мав першого повноцінного живого запуску, службові
# файли, які він САМ СТВОРЮЄ Й ПЕРЕЗАПИСУЄ (дедуп-логи, метрики,
# самоперевірка), створюються під ІНШИМИ назвами — з цим суфіксом — щоб не
# писати/читати ті самі Google-документи, якими вже керує щоденна
# scheduled-задача (текстова версія промпту). Без цього обидві системи
# заважали б одна одній: дедуп однієї "з'їдав" би вакансії, щойно показані
# іншою, і порівняння результатів було б брудним.
#
# TRACKER_SHEET_TITLE (нижче) свідомо БЕЗ цього суфікса — це ручна таблиця
# Володимира, у неї немає тестової копії: пайплайн читає з неї URL (Рівень 2
# дедублікації) і точково дописує статуси за листами з Кроку 2
# (pipeline/step3_tracker.py, вимикається TRACKER_AUTO_UPDATE).
#
# Коли довіра до Python-версії підтвердиться і прийде час передати їй
# канонічний журнал — постав тут "" (порожній рядок): наступний запуск
# почне писати в ті самі файли, що й чат-версія, продовжуючи той самий
# журнал (README, "Приклад реального запуску").
SERVICE_FILE_TITLE_SUFFIX = " (тест гітхаб)"

# Базові (канонічні) назви — БЕЗ суфікса. Потрібні Кроку 5 (порівняння), щоб
# читати РЕАЛЬНІ файли чат-версії незалежно від SERVICE_FILE_TITLE_SUFFIX
# вище — Крок 5 порівнює саме канонічний журнал з тестовим, тож завжди має
# бачити обидві назви одночасно.
DEDUP_LOG_TITLE_CANONICAL = "Лог показаних вакансій (автопошук)"
METRICS_SHEET_TITLE_CANONICAL = "Метрики автопошуку вакансій"
SELFCHECK_SHEET_TITLE_CANONICAL = "Результат щоденної перевірки"
FREELANCE_DEDUP_LOG_TITLE_CANONICAL = "Лог показаних фріланс-проєктів (автопошук)"
FREELANCE_METRICS_SHEET_TITLE_CANONICAL = "Метрики автопошуку фріланс-проєктів"

DEDUP_LOG_TITLE = DEDUP_LOG_TITLE_CANONICAL + SERVICE_FILE_TITLE_SUFFIX
METRICS_SHEET_TITLE = METRICS_SHEET_TITLE_CANONICAL + SERVICE_FILE_TITLE_SUFFIX

# Рівень 2 страховки дедублікації (якщо основний лог недоступний навіть
# після повторної спроби) — таблиця, яку Володимир заповнює вручну.
# Звіряємо тільки по URL-колонці, НЕ по назві компанії — та сама компанія
# легітимно публікує кілька різних вакансій (прецедент: два окремі записи
# Plamigo з різними URL в цій самій таблиці).
#
# Заголовки колонок — у РЯДКУ 2 (рядок 1 — об'єднана шапка "Interview —
# ПІП студента"), sheets.find_header_row шукає їх у перших 5 рядках.
TRACKER_SHEET_TITLE = "Ворошилов відгуки на вакансії"
TRACKER_SHEET_NAME = "Interview"  # вкладка з відгуками
TRACKER_URL_COLUMN_HEADER = "Посилання на вакансію"
TRACKER_COMPANY_COLUMN_HEADER = "Компанія"
TRACKER_RESULT_COLUMN_HEADER = "Результат відгуку"
TRACKER_NOTES_COLUMN_HEADER = "Примітки"

# Крок 3 (таблиця відгуків): автооновлення за листами Кроку 2 — "відмова"
# ставить "Результат відгуку" = відмова + примітку, "запрошення на
# співбесіду" — лише примітку. Кілька рядків однієї компанії без явної
# вказівки на вакансію -> пропуск (див. pipeline/step3_tracker.py).
TRACKER_AUTO_UPDATE = True

# Файл Кроку 4 — самоперевірка запуску (аудит-лог, без обмеження за
# довжиною, на відміну від дедуп-логу).
SELFCHECK_SHEET_TITLE = SELFCHECK_SHEET_TITLE_CANONICAL + SERVICE_FILE_TITLE_SUFFIX

# Крок 5 — порівняння автоматичного (чат) і ручного (Python) запуску того
# самого дня. Один спільний файл, БЕЗ суфікса — сама його мета в тому, щоб
# зіставляти обидві системи, тож він не потребує "тестової" версії себе
# самого. Пишеться лише поки SERVICE_FILE_TITLE_SUFFIX непорожній (див.
# pipeline/step5_comparison.py) — коли Python перейме канонічний журнал,
# порівнювати вже нема з чим.
COMPARISON_SHEET_TITLE = "Порівняльна таблиця автоматичних запусків та запусків вручну"

# Файл, який пайплайн НІКОЛИ не повинен чіпати — особистий ручний трекер
# співбесід Володимира. Тримаємо тут явно, щоб будь-який код, що працює
# з Drive-файлами, міг звірятися з цим списком-забороною.
FORBIDDEN_FILE_IDS = {
    "1wNsGBUKFvvliEquRufynZrCuxWbEEx8l": "Мої відгуки на вакансію.xlsx (ручний трекер співбесід)",
}

GMAIL_LABEL_NAME = "Знайдено-Пошук-Вакансій"

# 21 день — свідомий запас на пропущені запуски (не 7: тижнева перерва між
# запусками — реалістичний сценарій, і 7-денне вікно давало б дублі після
# будь-якої паузи довшої за тиждень). Якщо між запусками пройшло більше
# 21 дня — дедублікація за цей проміжок не гарантована, це прийнятний
# компроміс заради компактності файлу.
DEDUP_LOG_MAX_AGE_DAYS = 21

# --------------------------------------------------------------------------
# Часові пояси
# --------------------------------------------------------------------------
# Дві навмисно РІЗНІ дати в кожному запуску — не плутати:
#   - UTC (через datetime.now(timezone.utc)) — дата/час ЗАПУСКУ скрипта,
#     пишеться в усі три "бухгалтерські" файли (лог дублів, метрики,
#     результат щоденної перевірки), щоб їх можна було зіставляти напряму
#     без конвертації.
#   - CANDIDATE_LOCAL_TZ (Europe/Warsaw, CET/CEST) — використовується ЛИШЕ
#     для оцінки "сьогодні/вчора" при фільтрації дати ПУБЛІКАЦІЇ вакансії
#     (Крок 1) — це про те, як людина в Zawadzkie сприймає свіжість
#     вакансії, а не про бухгалтерію запуску скрипта.
CANDIDATE_LOCAL_TZ = "Europe/Warsaw"

# --------------------------------------------------------------------------
# Профіль кандидата
# --------------------------------------------------------------------------
@dataclass(frozen=True)
class CandidateProfile:
    name: str = "Володимир Ворошилов"
    location: str = "Zawadzkie, Польща"
    target_roles: tuple[str, ...] = ("Junior Data Analyst", "Junior Product Analyst")
    background: str = (
        "8+ років бекграунду в telecom/ops до переходу в аналітику даних, "
        "пройшов курс GoIT Data Analyst (сертифікат «Пройдено»)"
    )
    real_skills: tuple[str, ...] = (
        "SQL", "Python/Pandas", "Tableau", "EDA", "A/B тестування", "Excel",
    )
    # Навички, які кандидат НЕ має на практиці — не можна зараховувати їх
    # у Match-рівень і не можна приписувати кандидату.
    non_skills: tuple[str, ...] = ("Power BI", "Looker Studio", "R")


CANDIDATE = CandidateProfile()

# --------------------------------------------------------------------------
# Критерії фільтрації вакансій (Крок 1)
# --------------------------------------------------------------------------
MAX_EXPERIENCE_YEARS = 1.5
MIN_SALARY_UAH = 40_000
MAX_ENGLISH_LEVEL = "B2"  # виключає C1+
DATE_WINDOW_DAYS = 2  # "сьогодні або вчора"
# Дату не завжди вдається розпізнати (ні скрапер, ні Claude не витягли
# вказівки з posted_raw/description_snippet) — вікно "сьогодні/вчора" тоді
# не застосовне, і вакансія свідомо НЕ відкидається автоматично (могла бути
# свіжою). Це ліміт на ПОКАЗ: скільки вакансій з невизначеною датою
# публікації пропускається у фінальний звіт за один запуск
# (pipeline/step1_vacancies.py::run_step1) — решта понад ліміт відфільтровується,
# а не "скільки перевіряти/оцінювати" (Claude оцінює їх усі однаково, ліміт
# застосовується вже після оцінки, на етапі відбору).
UNKNOWN_DATE_FALLBACK_LIMIT = 50

# Скільки вакансій/проєктів Claude оцінює за один виклик API (crop 1 і
# крок 1.2). Виділено в конфіг, а не захардкоджено в pipeline-модулях, щоб
# можна було зменшити без правки коду, якщо контекст-вікно чи бюджет
# виклику стане тісним.
CLAUDE_EVAL_CHUNK_SIZE = 40

SOURCES = ("djinni.co", "jobs.dou.ua", "robota.ua", "work.ua", "happymonday.ua")

# --------------------------------------------------------------------------
# Крок 1.2 — фріланс-проєкти (Freelancehunt + Telegram-канал)
# --------------------------------------------------------------------------
# Мета — не пошук роботи за наймом, а тренувальні/практичні фріланс-проєкти
# (невеликий обсяг, 1-2 проєкти на тиждень, напрацювання практики й
# портфоліо). Виконується завжди одразу після Кроку 1, незалежно від його
# результату — та сама логіка "Крок 2 виконується завжди", що й у main.py.
FREELANCE_DEDUP_LOG_TITLE = FREELANCE_DEDUP_LOG_TITLE_CANONICAL + SERVICE_FILE_TITLE_SUFFIX
FREELANCE_METRICS_SHEET_TITLE = FREELANCE_METRICS_SHEET_TITLE_CANONICAL + SERVICE_FILE_TITLE_SUFFIX

# 10 днів, коротше вікно ніж 21 день у вакансіях — фріланс-проєкти
# закриваються швидше, довше вікно тримало б у логу вже неактуальні записи.
FREELANCE_DEDUP_LOG_MAX_AGE_DAYS = 10

# Ручний трекер заявок на фріланс — Володимир сам заповнює № / посилання /
# текст пропозиції / рішення (дропдаун На розгляді/Відхилено/Погоджено з
# кольоровим форматуванням, додано 24.09.2026) / примітки. Пайплайн
# НІКОЛИ не пише сюди автоматично — рядок з'являється лише тоді, коли
# Володимир прямо підтверджує, що подав заявку на конкретний проєкт. Той
# самий принцип, що й TRACKER_SHEET_TITLE вище для вакансій: файл існує
# для читання людиною й точкових правок у чаті, а не для автозапису
# скриптом (див. pipeline/step1_2_freelance.py, докладний коментар вгорі
# модуля, і README, розділ "Відомі обмеження").
FREELANCE_APPLICATIONS_SHEET_TITLE = "Ворошилов заявки на фріланс"

# Freelancehunt: ТІЛЬКИ ці дві категорії (перевірено пілотним запуском
# 22.09.2026 — інші категорії типу "Python" дають переважно нерелевантну
# веброзробку). Пошук за довільними ключовими словами через /search/ НЕ
# використовується — заблокований robots.txt.
FREELANCEHUNT_CATEGORIES = {
    "BI и аналитика данных": "https://freelancehunt.com/projects/skill/bi-analitika-dannyih/199.html",
    "Базы данных и SQL": "https://freelancehunt.com/projects/skill/bazyi-dannyih/86.html",
}

# Пороги конкуренції для Freelancehunt (config, не Claude — детерміновано),
# відкалібровані на реальному розподілі пілотного запуску 22.09.2026
# (6-117 ставок, медіана ~27).
# Офіційне API Freelancehunt v2 (https://apidocs.freelancehunt.com/) —
# основний шлях, якщо в .env задано FREELANCEHUNT_API_TOKEN (Налаштування
# профілю -> API). Стабільний JSON замість HTML, без антибот-блокувань.
# Без токена — фолбек на HTML-скрапінг категорій вище (через curl_cffi).
FREELANCEHUNT_API_TOKEN = os.environ.get("FREELANCEHUNT_API_TOKEN", "")
FREELANCEHUNT_API_URL = "https://api.freelancehunt.com/v2/projects"
# id навичок = числа в кінці URL категорій вище (.../199.html, .../86.html)
FREELANCEHUNT_SKILL_IDS = {
    "BI и аналитика данных": 199,
    "Базы данных и SQL": 86,
}

FREELANCEHUNT_LOW_BIDS_MAX = 14  # < 15 -> "низька"
FREELANCEHUNT_MEDIUM_BIDS_MAX = 40  # 15-40 -> "середня", > 40 -> "висока"

# Джерело 3 — загальний потік фріланс-гіґів (переважно Upwork-оголошення),
# без категорій, тому фільтр релевантності тут виключно за ключовими
# словами в тексті поста (на відміну від Freelancehunt, де це категорія).
# Веб-preview без логіну й без окремого конектора.
TELEGRAM_FREELANCE_CHANNEL = "freelance_jobs_motivation"
TELEGRAM_FREELANCE_PREVIEW_URL = f"https://t.me/s/{TELEGRAM_FREELANCE_CHANNEL}"
TELEGRAM_RELEVANCE_KEYWORDS = (
    "sql", "python", "excel", "google sheets", "дані", "data", "аналітика",
    "analysis", "analytics", "bi", "звітність", "reporting", "dashboard",
)

# Той самий "останні 1-2 дні" віконний фільтр, що й Крок 1 (DATE_WINDOW_DAYS
# вище), місцевий час кандидата (CANDIDATE_LOCAL_TZ) — однаково для всіх
# трьох джерел цього кроку.
FREELANCE_DATE_WINDOW_DAYS = DATE_WINDOW_DAYS

# Сигнал (не команда діяти автоматично) для ручного перегляду разом із
# Володимиром, якщо якесь із трьох джерел 7+ днів поспіль показує 0
# релевантних результатів — може означати, що джерело справді малоактивне,
# або що критерії пошуку варто переглянути.
FREELANCE_ZERO_STREAK_REVIEW_THRESHOLD = 7

# --------------------------------------------------------------------------
# Крок 4 — самоперевірка запуску
# --------------------------------------------------------------------------
# Поріг для евристичного сигналу "3+ запуски поспіль — лише Low/нуль при
# повному 5/5 покритті" — привід переглянути фільтри, а не статистично
# обґрунтоване правило. Поріг обрано довільно як практичний баланс для
# ручного щотижневого моніторингу; підлаштуй, якщо на практиці виявиться
# занадто чутливим чи навпаки.
LOW_MATCH_STREAK_HEURISTIC_RUNS = 3
SELFCHECK_TREND_LOOKBACK_ROWS = 5  # скільки останніх рядків читати для евристики

# --------------------------------------------------------------------------
# Push-повідомлення (безумовне, наприкінці кожного запуску)
# --------------------------------------------------------------------------
# Чат-версія промпту надсилала push через нативний інструмент телефону
# користувача — у самостійному Python-скрипті такого немає "з коробки".
# Тут — простий, безкоштовний і не залежний від Google-облікового запису
# шлях через ntfy.sh (https://ntfy.sh, публічний pub/sub, без реєстрації):
# піднімається один HTTP POST на власну тему. Якщо PUSH_NTFY_TOPIC не
# задано — сповіщення пропускається з попередженням у лог, запуск не
# падає через це (як і всі інші необов'язкові кроки пайплайна).
PUSH_NTFY_TOPIC = os.environ.get("PUSH_NTFY_TOPIC", "")
PUSH_NTFY_SERVER = os.environ.get("PUSH_NTFY_SERVER", "https://ntfy.sh")
PUSH_MESSAGE_TEXT = "Щоденну перевірку завершено, можете ознайомитись з результатами пошуку"

# --------------------------------------------------------------------------
# Крок 2 — пошта
# --------------------------------------------------------------------------
GMAIL_SEARCH_WINDOW_DAYS = 30
GMAIL_KEYWORDS = (
    "вакансія", "співбесіда", "candidate", "interview", "resume", "application",
)
# Токени, які потрібно виключити з назви файлу при витягуванні назви компанії
FILENAME_NOISE_TOKENS = (
    "cv", "resume", "cover", "cover_letter", "cover letter", "voroshylov",
    "ворошилов", "data", "analyst", "business", "marketing", "junior",
    "middle", "senior", "product",
)

EMAIL_STATUS_OPTIONS = (
    "відгук на розгляді",
    "запрошення на співбесіду",
    "відмова",
    "headhunting-пропозиція (кандидат не подавався)",
    "очікування без явної відповіді",
)
